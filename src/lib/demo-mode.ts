/**
 * Demo mode: sign-in is bypassed so reviewers can open the prototype directly.
 * Flip to `false` to restore staff authentication on the UI, the server
 * functions and the /api/chat endpoint.
 */
export const DEMO_OPEN_ACCESS = true;
